# Kopieren nach terraform.tfvars (gitignored) und Werte einsetzen.
project               = "ffn-utils"
build_service_account = "cloudbuild@ffn-utils.iam.gserviceaccount.com"
github_org            = "r00tat"

# Nur setzen, wenn dev ein eigenes GCP-Projekt bekommt — dann zusätzlich
# project und state_bucket auf das neue Projekt umstellen und den Prefix in
# backend.tf anpassen.
# manage_project_base = true
