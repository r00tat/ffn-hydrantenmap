{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        ".",
        "--only",
        "core,firestore,storage,auth,messaging,functions,remoteconfig,database,developerknowledge,crashlytics"
      ]
    }
  }
}
