'use client';

import type { NextPage } from 'next';
import AtemschutzWrapper from '../../components/pages/AtemschutzWrapper';

const Page: NextPage = () => {
  return <AtemschutzWrapper />;
};

export default Page;
